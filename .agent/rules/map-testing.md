---
trigger: when testing or interacting with the map canvas
---

When interacting with the map canvas in the browser:
- **Scrolling**: Do NOT use standard wheel scrolling. Instead, use a **click-and-drag** motion to move the map. To move down the map (towards the start), click and drag upwards.
